{
    /*
      "include" 用来指定哪些 ts 文件需要被编译
          路径：** 表示任意目录，*表示任意文件
          默认值：["** / *"]，表示所有文件都需要被编译
      "exclude" 不需要被编译的文件目录
          默认值：["node_modules", "bower_components", jspm_packages]
      "files" 指定被编译的文件，只有文件少时才会用到
      "compilerOptions" 编译器选项
  */
    "compilerOptions": {
        // module 指定要使用的模块化的规范（使用的模块化规范，简单来说就是js代码的组织方式）
        // none, commonjs, amd, system, umd, es6, es2015, es2020, esnext
        "module": "es6",
        // target 用来指定ts被编译为的es版本（使用的ECMA的语言特性版本，简单来说就是js代码的兼容性）
        // es3, es5, es6, es2015, es2016, es2017, es2018, es2019, es2020,
        "target": "es6",
        // outDir 用来指定编译后文件所在的目录
        "outDir": "./dist",
        //// outFile 将代码合并为一个文件(只有amd或system模块化时才有效)
        // "outFile": "./dist.app.js",

        "sourceMap": true
    },
    "exclude": [
        "node_modules"
    ]
}
